# v1.1 Followup（v1.0.1 留下）

> v1.0.1 在不擴大 v1.0 範圍下完成 UX/設計系統升級 + 後端缺口修補。本文件列出明確留給 v1.1+ 的事項與來源說明。

## 一、留待 prod 觀察才能評估

| 項目 | 原因 | 啟動條件 |
|------|------|---------|
| `data_freshness_minutes` SLO 基準 | dev DB 無真實 prod OHLCV 流量 | prod 跑 30 天後讀 `docs/slo_reports/*.json` 取 p95 |
| `audit_integrity` SLO 在 prod 表現 | v1.0 dev DB 有 tampering 測試殘留（已用 [scripts/dev_cleanup_audit_artefacts.py](../../backend/scripts/dev_cleanup_audit_artefacts.py) 清掉）；prod 啟動時是乾淨表 | 啟動後 30 天 verify_audit_chain 任務有跑且 broken_id=0 |
| HSTS 開啟 | self-signed cert 開了會鎖死瀏覽器 | 有真實憑證後在 `nginx.conf` 取消註解 |

## 二、v1.0.1 啟動準備

| 項目 | 命令 |
|------|------|
| 套用 0017 migration | `cd backend && uv run alembic upgrade head` |
| 清 dev DB audit 殘留 | `make dev-cleanup-audit ARGS="--yes"` |
| 重建前端 | `cd frontend && npm install && npm run build` |
| 驗證 | `cd backend && uv run pytest -q` （預期 718 passed / 3 skipped）|
|  | `cd frontend && npm run test` （預期 209 passed） |
|  | `cd frontend && npm run build` |

## 三、UI 仍可改善但留 v1.1（避免擴大 v1.0 範圍）

### v1.1（1-2 個月）
- 大盤 OHLCV symbol `TAIEX` / `TPEX` 真實 backfill（Dashboard sparkline 才會有真資料；目前 fallback 提示）
- `AnalysisService.run_analysis` 寫入 `analyst_outputs` JSONB（schema 已備好，但 Celery task 還沒寫入；目前 detail.analyst_outputs 永遠 null）
- calendar / compare / backtest 真實後端（v1.0 為 mock）
- Email 通知（含 password reset）
- 自動匯出分析到 Obsidian

### v1.2（3-6 個月）
- 英文 UI 完整翻譯（v1.0.1 只補了 zh-TW 字典，en 仍 fallback）
- 2FA TOTP / IP 白名單
- LINE Bot 雙向

### v2.0（6-12 個月）
- 真實券商 API（仍手動核准）
- Prometheus + Grafana + OpenTelemetry
- 多用戶多組織 / K8s / 行動 App

## 四、bandit MEDIUM 4 項（已審查）

仍維持 v1.0 接受清單，詳見 [SECURITY.md](../../SECURITY.md) 「v1.0 已接受風險」。

## 五、清單範本（PR 描述用）

```markdown
- [ ] alembic upgrade head（套用 0017）
- [ ] dev DB 跑 dev-cleanup-audit
- [ ] backend pytest 全綠（718+）
- [ ] frontend typecheck / vitest / next build 全綠
- [ ] 視覺檢查：dashboard / analysis/[id] / login / market/overview
- [ ] mobile（<768px）：sidebar sheet 開關正常
- [ ] ⌘K：股票搜尋 / 跳頁 / 最近分析
- [ ] 黑暗模式色彩協調
```
